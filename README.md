# node-opencv-rec-facial
📦Um pacote com três nodes (NodeRED) para reconhecimento facial 


Instalar:

OpenCV

Python, pip e pacote python do OpenCV

NodeRED

Dentro do nodeRED, baixar os nodes:
  
  node-red-dashboard
  
  node-red-node-arduino
  
  node-red-contrib-browser-utils
  
  opencv-rec-facial (Baixar do git. Ir até a pasta ~/.node-red e digitar: npm install [diretório da pasta que você baixou])


OBS.: Essa biblioteca ainda não está pronta para uso, deve-se fazer algumas alterações nos diretórios dentro do programa para usar no seu computador

OBS2.: Apesar da data, esse é o mesmo repositório usado no TDC, eu exclui por engano.
